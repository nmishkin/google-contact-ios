import SwiftUI
import GoogleContactsKit

// Temporary stub -- replaced with the real detail view in Task 11.
struct ContactDetailView: View {
    let contact: Contact
    var body: some View { Text(contact.givenName) }
}
